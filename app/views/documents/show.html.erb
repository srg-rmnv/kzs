<div class="row">
	
	<%= render 'left' %>
	
	<div class="span8">
		
		<div class="btn-toolbar">
			<div class="btn-group">	
				<%= link_to " назад", documents_path, :class => "btn" %>
			</div>
			
			<div class="btn-group">	
				<a class="btn btn-success" href="javascript:if(window.print)window.print()" style="margin-left: 20px"><%= image_tag('print.png') %></a>
			</div>
		</div>
		
		<div class="new-wrap">
			<%= render @document %>
	    </div>	
	
		<% if @document.document_type == 'writ' %>
		<div class="new-wrap">
			<% if @document.statements.empty? %>
			  <h5>Акты отсутствуют</h5>
			<% else %>
			  <h5>Акты</h5>
			  <ul>
			  <% @document.statements.each do |statement| %>
			  <li><%= link_to statement.title, statement_path(statement) %></li>
			  <% end %>
			  </ul>
			<% end %>
	    </div>
		<% end %>
		
	
    </div>


</div>