<div class="row">
	
	<%= render 'left' %>
	
	<div class="span8">
		
		<div class="btn-toolbar">
			<div class="btn-group">	
				<%= link_to " назад", :back, :class => "btn" %>
			</div>
			
			<div class="btn-group">	
				<a class="btn btn-success" href="javascript:if(window.print)window.print()" style="margin-left: 20px"><%= image_tag('print.png') %></a>
			</div>
		</div>
		
		<div class="new-wrap">
			<%= form_for(@document) do |f| %>
			<div class="new-header">
				<span class="header"><%= @document.title %> | <%= Russian::strftime(@document.created_at, "%d %B %Y, %H:%M") %></span>
				<span class="label label-info" style="float: right; margin-top: 7px">Письмо</span>
			</div>
			
			<div class="new-title">
				<b>Отправитель: </b><%= link_to doc_user(@document.user_id), user_path(@document.user_id) %>
				<b style="margin-left: 40px;">Ответственный: </b><%= link_to doc_user(@document.recipient_id), user_path(@document.recipient_id) %>
				<b style="margin-left: 40px;">Согласующий: </b><%= link_to doc_user(@document.approver_id), user_path(@document.approver_id) %>
			</div>
			
			<% if @document.organization_id? %>
			<div class="new-title"><b>Организация: </b><%= link_to organization(@document.organization_id), organization_path(@document.organization_id) %><!--<span style="margin-left: 60px;">Исходящий №</span><input class="input-medium" type="text" placeholder=".input-medium">--></div>
			<% end %>
			
			<div class="new-text-wrap">
				<%= @document.text.html_safe %>
			</div>
			
			
			<div class="btn-toolbar">
				<% unless @document.sent? || @document.deleted? %>
				<div class="btn-group"   style="margin-right: 10px;">	
					<%= link_to 'Редактировать', edit_document_path(@document), :class => "btn btn-large btn-success" %>
				</div>
				<% end %>
				
				<% if for_approve(@document) %>
				<div class="btn-group"   style="margin-right: 10px;">	
					<%= link_to 'Одобрить', approve_document_path(@document), :class => "btn btn-large btn-success" %>
				</div>
				<% end %>
				
				<% if for_callback(@document) %>
				<div class="btn-group"   style="margin-right: 10px;">	
					<%= link_to 'Отозвать', callback_document_path(@document), :class => "btn" %>
				</div>
				<% end %>
				
				<% unless @document.sent? || @document.deleted? %>
				<div class="btn-group"   style="margin-right: 10px;">	
					<%= link_to 'Удалить', delete_document_path(@document), :class => "btn btn-danger" %>
				</div>
				<% end %>
			
			</div>
		
			<% end %>
	</div>	
</div>
